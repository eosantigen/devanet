# Use the hwaddress address identified by 'ip addr show eno1'.
# 'ip route'
# If you want to turn on Spanning Tree Protocol, ask your hosting
# provider first as it may conflict with their network.

iface eno1 inet manual

auto br0

iface br0 inet static
    hwaddress ether 40:8d:5c:e0:01:0b
    address 192.168.1.5 # static
    netmask 192.168.1.255
    gateway 192.168.1.254

    bridge_ports eno1
    bridge_stp off

    # If STP is off, set to 0. If STP is on, set to 2 (or greater).
    bridge_fd 0